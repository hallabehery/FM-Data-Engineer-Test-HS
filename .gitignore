# --- Internal hiring material — never distribute to candidates ---
_internal/

# --- DuckDB warehouse ---
# The populated submission/warehouse.duckdb IS committed, per SETUP.md §8 ("hand back the
# populated warehouse — commit it or zip the lot"). It's a build output (`make pipeline`),
# never hand-edited. Only the write-ahead log stays ignored.
warehouse.duckdb.wal
*.duckdb.wal

# Note: submission/star_map.html IS committed — it's a small (~700 KB), self-contained
# illustrative deliverable a reviewer can open straight from the repo (regenerate with
# `make render`). Unlike the multi-MB warehouse binary above, it's worth versioning.

# --- Python ---
.venv/
venv/
env/
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# --- Jupyter ---
.ipynb_checkpoints/
.jupyter/
*.ipynb_checkpoints

# --- Local tooling noise (agent sessions, pytest cache) ---
.claude/
.pytest_cache/

# --- OS / editor noise ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
