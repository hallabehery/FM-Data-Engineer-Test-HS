# --- Internal hiring material — never distribute to candidates ---
_internal/

# --- DuckDB warehouse (candidates commit their own; keep the repo template clean) ---
# Comment the next line out if you want to hand back a pre-populated warehouse.
warehouse.duckdb
warehouse.duckdb.wal
*.duckdb
*.duckdb.wal

# --- Illustrative render (build output — regenerate with `make render`) ---
submission/star_map.html

# --- Python ---
.venv/
venv/
env/
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# --- Jupyter ---
.ipynb_checkpoints/
.jupyter/
*.ipynb_checkpoints

# --- OS / editor noise ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
